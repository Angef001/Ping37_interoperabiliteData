# Chemin vers les fichiers EDS
EDS_DIR=C:\Projets\Ping\fhir_eds_api\Ping37_interoperabiliteData\data\eds

# Chemin vers le mapping JSON
FHIR_MAPPING_PATH=C:\Projets\Ping\fhir_eds_api\Ping37_interoperabiliteData\app\core\config\mapping.json

# Dossier de sortie FHIR
FHIR_OUTPUT_DIR=C:\Projets\Ping\fhir_eds_api\Ping37_interoperabiliteData\exports_eds_fhir

#Ici "patient" peut être remplacé par "encounter" si on veut un bundle par encounter
FHIR_BUNDLE_STRATEGY=patient

#Définis l'url de l'entrepôt fhir
DEFAULT_FHIR_URL=http://localhost:8080/fhir


REPORTS_DIR_EDS = os.getenv("REPORTS_DIR", str(PROJECT_ROOT / "data" / "reports")
